<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="java.util.Date"/>
        <variable
            name="jesta"
            type="com.example.jesta.GetFavorsByRadiosTimeAndDateQuery.GetByRadiosAndDateAndOnlyAvailable " />
        <variable
            name="viewModel"
            type="com.example.jesta.viewmodel.MapViewModel" />
        <variable
            name="myLocation"
            type="com.google.android.gms.maps.model.LatLng" />
        <variable
            name="srcDate"
            type="String" />
        <variable
            name="destDate"
            type="String" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->viewModel.markerClicked(jesta._id)}"
        >

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:text="נקיון הבית"
            android:textStyle="bold"
            android:layout_marginTop="@dimen/margin_vertical"
            android:layout_marginStart="@dimen/margin_side"
            android:textColor="?attr/colorPrimary"
            />

        <TextView
            android:id="@+id/distance_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/title"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="@dimen/margin_side"
            app:jestaLocation="@{jesta.sourceAddress.location.coordinates}"
            app:myLocation="@{myLocation}"
            />

        <TextView
            android:id="@+id/time_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/title"
            app:layout_constraintStart_toStartOf="@id/title"
            sourceDate="@{srcDate}"
            destDate="@{destDate}"
            android:layout_marginTop="@dimen/margin_vertical"
            android:textColor="@color/gray700"
            />

        <ImageView
            android:id="@+id/location_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/time_title"
            app:layout_constraintStart_toStartOf="@id/title"
            android:layout_marginTop="@dimen/margin_vertical"
            android:background="@drawable/ic_baseline_location_orange_24"
            />

        <TextView
            android:id="@+id/location_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="5dp"
            android:text="@{jesta.sourceAddress.fullAddress}"
            android:textSize="11sp"
            app:layout_constraintBottom_toBottomOf="@+id/location_icon"
            app:layout_constraintStart_toEndOf="@id/location_icon"
            app:layout_constraintTop_toTopOf="@id/location_icon" />

        <ImageView
            android:id="@+id/amount_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="@id/distance_title"
            app:layout_constraintTop_toTopOf="@id/location_icon"
            android:background="@drawable/ic_baseline_group_24"
            />

        <TextView
            android:id="@+id/amount_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toStartOf="@id/amount_icon"
            app:layout_constraintTop_toTopOf="@id/amount_icon"
            app:layout_constraintBottom_toBottomOf="@id/amount_icon"
            android:text="@{String.valueOf(jesta.numOfPeopleNeeded)}"
            android:layout_marginEnd="2dp"
            />


        <com.google.android.material.divider.MaterialDivider
            android:id="@+id/bottom_dvider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_vertical"
            app:layout_constraintTop_toBottomOf="@id/location_icon"
            />





    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>